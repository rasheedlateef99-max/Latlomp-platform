# LATLOMP E9 — COMMUNITY VERIFICATION CHECKLIST

Complete this manually after running e9.tenant.test.js.
Tick each item after confirming it passes on your instance.
Use two test school accounts: School A and School B.

---

## SECTION 1 — SETUP PREREQUISITES

- [ ] Two test schools configured and accessible
- [ ] Both schools have Google OAuth working
- [ ] Community is enabled on School A (`isEnabled: true`)
- [ ] Community is enabled on School B
- [ ] School A has at least 5 test posts in various states:
      published, pending (if approval mode on), locked
- [ ] School A has at least 3 members: admin, moderator, regular member
- [ ] A suspended member account exists for Section 4 tests
- [ ] School A has at least 2 posts with comments and replies
- [ ] Railway/production logs accessible for email delivery check

---

## SECTION 2 — AUTHENTICATION

### Community token issuance
- [ ] School A admin logs into school dashboard → clicks Community →
      community opens and shows the feed (token auto-issued via /auth/join/staff)
- [ ] localStorage.latlomp_community_token is set after community load
- [ ] Refreshing the community page does NOT require re-login
      (token persisted in localStorage and refreshed if near expiry)
- [ ] Logging out of the institution dashboard, then navigating to /community/
      shows the "Not Authenticated" screen (no community token available)

### Wrong token types
- [ ] Manually clearing latlomp_community_token in localStorage
      and visiting /community/ → "Not Authenticated" screen appears
- [ ] Calling GET /api/community/feed with latlomp_inst_token
      as Bearer token → 401 "Invalid token type" (not 200)
- [ ] Calling GET /api/community/feed with a completely expired
      token → 401 "expired" response

---

## SECTION 3 — TENANT ISOLATION

### Two-school test
- [ ] School A admin: community shows School A posts, not School B's
- [ ] School B admin: community shows School B posts, not School A's
- [ ] School A community name shows in topbar (not School B's name)
- [ ] POST a test post in School A community →
      it does NOT appear in School B's feed
- [ ] GET /api/community/feed with School A token →
      response never includes School B content

### IDOR — using real IDs
- [ ] Take a post ObjectId from School A's feed
- [ ] Make this request with School B's community token:
      POST /api/community/posts/<schoolA_postId>/comments
      → must return 404 or 403, NOT 201
- [ ] POST /api/community/posts/<schoolA_postId>/react
      with School B's token → 404 or 403, NOT 200
- [ ] POST /api/community/posts/<schoolA_postId>/report
      with School B's token → 404 or 403, NOT 201
- [ ] PUT /api/community/admin/members/<schoolA_memberId>/role
      with School B's admin token → 404 or 403

---

## SECTION 4 — SUSPENSION ENFORCEMENT

Setup: suspend a test member using the admin panel.

### Backend enforcement (most important)
- [ ] Suspended member: GET /api/community/feed
      → 200 (reads allowed while suspended)
- [ ] Suspended member: GET /api/community/posts/:id/comments
      → 200 (reads allowed)
- [ ] Suspended member: POST /api/community/posts
      → 403 "suspended" response (write blocked server-side)
- [ ] Suspended member: POST /api/community/posts/:id/comments
      → 403 (comment write blocked)
- [ ] Suspended member: POST /api/community/posts/:id/react
      → 403 (reaction write blocked)
- [ ] Suspended member: POST /api/community/media/upload
      → 403 (media upload blocked)

### Frontend enforcement
- [ ] Suspended member loads community → composer is hidden or shows warning
- [ ] Suspended member sees "Your community write access is suspended" banner
- [ ] Suspension notice shows "until [date]" if time-limited

### Lifting suspension
- [ ] Admin unsuspends the test member via Manage Members panel
- [ ] Suspended member reloads → suspension warning disappears
- [ ] Suspended member can now POST /api/community/posts → 201

---

## SECTION 5 — MODERATION ROLE BOUNDARIES

### What regular members (role: member) CANNOT do
Using a community token with role=member:
- [ ] POST /api/community/admin/posts/:id/pin
      → 403 (moderator required)
- [ ] POST /api/community/admin/posts/:id/lock
      → 403 (moderator required)
- [ ] POST /api/community/admin/posts/:id/approve
      → 403 (moderator required)
- [ ] POST /api/community/admin/posts/:id/reject
      → 403 (moderator required)
- [ ] DELETE /api/community/admin/posts/:id
      → 403 (moderator required)
- [ ] DELETE /api/community/admin/comments/:id
      → 403 (moderator required)
- [ ] GET /api/community/admin/reports
      → 403 (moderator required)
- [ ] PUT /api/community/admin/settings
      → 403 (admin required)
- [ ] PUT /api/community/admin/members/:id/role
      → 403 (admin required)
- [ ] PUT /api/community/admin/members/:id/suspend
      → 403 (moderator required)

### What moderators CAN do
Using a community token with role=moderator:
- [ ] Can lock/unlock posts ✅
- [ ] Can remove posts and comments ✅
- [ ] Can approve/reject pending posts ✅
- [ ] Can suspend members ✅
- [ ] Cannot change community settings → 403 ✅
- [ ] Cannot change member roles → 403 ✅
- [ ] Cannot ban members → 403 ✅

---

## SECTION 6 — LOCKED DISCUSSION

- [ ] Admin locks a post via the admin panel
- [ ] POST /api/community/posts/:lockedPostId/comments
      → 403 "Discussion is locked" (not 201)
- [ ] GET /api/community/posts/:lockedPostId/comments
      → 200 (existing comments still visible)
- [ ] Frontend: comment box is hidden or disabled when post is locked
- [ ] Locked badge/indicator visible on the locked post
- [ ] Admin can unlock the post → comments accepted again

---

## SECTION 7 — POST APPROVAL WORKFLOW

Enable requirePostApproval in community settings.

- [ ] Member submits post → returns 201 with status: 'pending'
- [ ] Pending post does NOT appear in the main feed
- [ ] Pending post appears in /api/community/admin/posts/pending
- [ ] Pending post visible in Approval Queue tab for admin/mod
- [ ] Admin approves post → appears in feed, author notified by email
- [ ] Admin rejects post → does NOT appear in feed, author notified
- [ ] Member cannot see their own pending post in the feed
      (only the author who submitted it knows it's pending)

---

## SECTION 8 — REPORT SYSTEM

- [ ] Member can report another member's post: reason selector + submit
- [ ] Member CANNOT report their own post → 400 "cannot report own content"
- [ ] Second report on same post from same member → 400 (duplicate)
- [ ] Report appears in admin Reports Queue tab
- [ ] Admin dismisses report → status becomes 'dismissed'
      report disappears from pending queue
- [ ] Admin actions report → content removed, status 'actioned',
      all other pending reports on same content also resolved
- [ ] Reporting a comment works the same as reporting a post
- [ ] Content removed via report action is no longer visible in feed
- [ ] Restore: admin can restore a removed post via admin panel

---

## SECTION 9 — AUTO-LOCK THRESHOLD

Enable autoLockThreshold = 2 in Community Settings.

- [ ] One member reports a post → post stays unlocked (below threshold)
- [ ] Two different members report the same post →
      post is automatically locked
- [ ] POST /api/community/posts/:autoLockedId/comments
      → 403 (auto-lock enforced as a normal lock)
- [ ] Moderation log shows 'post_locked' entry with reason
      "Auto-locked: reached report threshold of 2"
- [ ] Admin can manually restore and unlock the auto-locked post

---

## SECTION 10 — CONTENT SAFETY

- [ ] Post content stored as plain text (no HTML tags in DB)
- [ ] XSS attempt: POST { content: '<script>alert(1)</script>' }
      → content stored and rendered as literal text, not executed
- [ ] XSS attempt in comment: same result
- [ ] XSS attempt in memberName (PUT /me): sanitized on save
- [ ] Media upload: rename .exe to .jpg and upload
      → rejected with error (magic bytes check)
- [ ] Image > 5MB → rejected (size limit)
- [ ] Video > 50MB → rejected (size limit)
- [ ] Uploaded image is served with correct Content-Type
- [ ] Community media upload: verify schoolId in DB comes from JWT
      (not from any form field)

---

## SECTION 11 — NOTIFICATIONS (E9H)

### Environment check
- [ ] Confirm which email provider is configured:
      - SENDGRID_API_KEY set → SendGrid
      - RESEND_API_KEY set → Resend
      - SMTP_HOST set → Nodemailer
      - None → dev console log mode (check server logs)
- [ ] EMAIL_FROM or COMMUNITY_EMAIL_FROM is set in Railway env vars

### Delivery tests (dev mode: check server console)
- [ ] Member B comments on Member A's post →
      server log shows "[community-notify] DEV EMAIL" (or real email sent)
      with subject "commented on your post"
- [ ] Member C replies to Member B's comment →
      Member B is notified (not Member A)
- [ ] Self-comment (member comments on own post) →
      NO notification email (self-notification prevented)
- [ ] Post approved by moderator →
      author receives "Post Approved" notification
- [ ] Post rejected by moderator →
      author receives "Post Not Approved" notification with reason
- [ ] Report actioned (content removed) →
      affected author receives "Content Removed" notification

### Toggle
- [ ] Admin sets notificationsEnabled = false in settings
- [ ] Member B comments on Member A's post →
      NO notification email (toggle respected)
- [ ] Admin re-enables notifications → delivery resumes

### Email content safety
- [ ] Notification email contains ONLY: member name, 80-char preview,
      community name, link to /community/
- [ ] No passwords, tokens, school IDs, or private data in email body
- [ ] HTML is properly escaped in email (no XSS in notification)

---

## SECTION 12 — FEED VISIBILITY

- [ ] Staff-only post (visibility: 'staff') is NOT returned in
      student member's GET /api/community/feed
- [ ] 'all' visibility post appears in all member types' feeds
- [ ] Pinned posts appear at the top of the feed regardless of date
- [ ] Maximum 5 pinned posts per school enforced
      (attempt to pin a 6th → error or oldest auto-unpinned)
- [ ] Removed posts (status: 'removed') never appear in feed
- [ ] Pending posts never appear in main feed

---

## SECTION 13 — MODERATION LOG

- [ ] Every mod action (approve, reject, lock, unlock, pin, unpin,
      remove post, remove comment, dismiss report, action report)
      creates an entry in CommunityModerationLog
- [ ] GET /api/community/admin/moderation/log returns entries
      newest-first, scoped to current school only
- [ ] Log entry contains: action, targetType, contentPreview,
      performedByName, createdAt
- [ ] contentPreview is truncated to 120 chars
- [ ] Log is append-only — no entry is ever modified or deleted
- [ ] School A's log never appears in School B admin's moderation log

---

## SECTION 14 — MEMBERSHIP LOG (E9F)

- [ ] New member joining community creates 'joined' log entry
- [ ] Role change creates 'role_changed' log entry
- [ ] Suspension creates 'suspended' log entry
- [ ] GET /api/community/admin/log returns correct entries

---

## SECTION 15 — E1–E8 REGRESSION

### Critical paths — verify these still work after E9
- [ ] Student portal loads at /institution/student/portal.html
- [ ] Parent portal loads at /institution/parent/dashboard.html
- [ ] School admin dashboard loads at /institution/school/dashboard.html
- [ ] Finance routes still functional (/api/institution/finance/...)
- [ ] Examination routes still functional
- [ ] Alumni portal (E6) routes still functional
- [ ] E8 school website builder at /institution/school/website.html
- [ ] E8 public website at /school/:slug (or 404 if unpublished)
- [ ] E8 management: GET /api/institution/website/ requires inst token
- [ ] E8 media upload still works via institution routes
- [ ] instProtect middleware: latlomp_inst_token still accepted on all E8 routes
- [ ] SchoolAnnouncement routes unaffected
- [ ] SchoolEvent routes unaffected

---

## SECTION 16 — PERFORMANCE SPOT CHECKS

- [ ] Feed with 50+ posts loads in under 1 second locally
- [ ] Comment thread with 20+ comments loads in under 500ms
- [ ] Member list with 100+ members paginates correctly
- [ ] Feed pagination: cursor advances correctly, no duplicates across pages
- [ ] Rapid consecutive reactions toggle correctly (no counter corruption)

---

## SIGN-OFF

Tester:  ___________________
Date:    ___________________
Server:  ___________________  (local / staging / Railway URL)

Automated: ___ / 30 tests passed
Manual:    ___ / 90+ checklist items

Notable findings:
________________________________________________________________
________________________________________________________________
________________________________________________________________

E9 release approved: ☐ YES  ☐ NO — issues to fix: ____________